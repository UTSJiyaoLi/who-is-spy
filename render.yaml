services:
  - type: web
    name: who-is-spy
    runtime: node
    plan: free
    buildCommand: npm install && cd server && npm install && cd .. && npm run build
    startCommand: npm run server
    envVars:
      - key: NODE_VERSION
        value: 20
